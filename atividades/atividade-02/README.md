# Atividade 02 — Produtor-Consumidor

Nesta atividade, executei o programa Produtor-Consumidor disponibilizado pelo professor. O programa possui um produtor, que adiciona itens ao buffer, e um consumidor, que retira esses itens.

Para compilar os arquivos, utilizei:

```bash
javac *.java
```

Depois, executei o programa com:

```bash
java Factory
```

Também adicionei meu nome no início da execução. O resultado pode ser conferido no arquivo `saida_atividade-02.png`.

**Aluno:** Gabriel Sales
